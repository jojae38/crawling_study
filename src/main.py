from pathlib import Path
import sys

# # 프로젝트 루트를 pathlib을 사용하여 계산
# project_root = Path(__file__).resolve().parent.parent

# # 프로젝트 루트를 sys.path에 추가
# sys.path.append(str(project_root))

from setup_env import *